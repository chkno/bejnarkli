* Verify blob's MAC
* Blobs are content-addressed (unnamed)
* Accept a blob over TCP
* Forward blobs to other servers
* Only forward once
* Send asynchronously
* Send with specified durability
* Use TOR
